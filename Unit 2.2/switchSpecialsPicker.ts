function getSpecials(weekday: string): string {
    switch(weekday) {
        case "Monday":
            return "cup of nud monday every cup has extra gummy worms"
            break;
        case "Tuesday":
            return "taco tuesday 50% off all tacos"
            break;
        case "Wednesday"
    }
}